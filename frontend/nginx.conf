server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # SPA fallback so client-side routing (if any is added later) still works.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
